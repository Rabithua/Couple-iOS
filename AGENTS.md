# Couple-iOS 工程规则

## 修改后安装到 huaweiair

完成任何会影响 App 运行产物的代码、资源或工程配置修改后，必须把更新构建并安装到用户的 `huaweiair` 设备。

- 优先使用用户本机的 `xcdev` 工具完成构建、安装或运行流程。
- 安装必须发生在修改和必要验证之后，最终回复里要明确说明安装到 `huaweiair` 是否成功。
- 如果当前任务只是阅读、审查、说明，或修改不影响 App 运行产物，可以不安装，但最终回复要说明没有触发安装的原因。
- 如果因为设备未连接、签名、构建失败或 `xcdev` 不可用导致无法安装，必须把具体阻塞点告诉用户，不要静默跳过。
